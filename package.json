{
  "name": "@lapse/root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "bun@1.3.0",
  "scripts": {
    "dev:web": "bun --filter web run dev",
    "build:web": "bun --filter web run build",
    "build:common": "bun --filter @lapse/common run build"
  },
  "devDependencies": {
    "turbo": "canary",
    "typescript": "^5"
  }
}

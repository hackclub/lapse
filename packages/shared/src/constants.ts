/**
 * The minimum size, in bytes, of a single recording session. Sessions at or below this threshold are
 * considered impossibly small and should be discarded.
 */
export const MIN_SESSION_SIZE_BYTES = 384;
